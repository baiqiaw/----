# TypeScript 类型定义包

共享的 TypeScript 类型定义，供后端、前端和桌面客户端使用。

## 使用方式

在其他包中引用：

```typescript
import { User, UserRole, TaskStatus } from '@document-converter/types';
```

## 构建

```bash
pnpm build
```

## 类型检查

```bash
pnpm type-check
```
